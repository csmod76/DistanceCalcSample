namespace CalculateDistanceApi
{
    /// <summary>
    /// Units Available & returned in the Calculations
    /// </summary>
    public enum DistanceUnits
    {
        Kilometers,
        Miles
    }
}
